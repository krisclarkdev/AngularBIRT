'use strict';

describe('AngularBIRT.savedFilters module', function() {

    beforeEach(module('AngularBIRT.savedFilters'));

    describe('savedFilters controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var SavedFiltersCtrl = $controller('SavedFiltersCtrl');
            expect(mainCtrl).toBeDefined();
        }));

    });
});