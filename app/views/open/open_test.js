'use strict';

describe('reportFilters.open module', function() {

  beforeEach(module('reportFilters.open'));

  describe('open controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var openCtrl = $controller('OpenCtrl');
      expect(openCtrl).toBeDefined();
    }));

  });
});