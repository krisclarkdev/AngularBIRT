'use strict';

describe('reportFilters.viewParameters module', function() {

    beforeEach(module('reportFilters.viewParameters'));

    describe('viewParameters controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var view1Ctrl = $controller('ViewParametersCtrl');
            expect(view1Ctrl).toBeDefined();
        }));

    });
});