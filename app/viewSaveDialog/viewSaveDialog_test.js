'use strict';

describe('reportFilters.viewSaveDialog module', function() {

    beforeEach(module('reportFilters.viewSaveDialog'));

    describe('viewSaveDialog controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewSaveDialogCtrl = $controller('ViewSaveDialogCtrl');
            expect(viewSaveDialogCtrl).toBeDefined();
        }));

    });
});