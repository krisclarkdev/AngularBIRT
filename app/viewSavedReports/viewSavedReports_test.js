'use strict';

describe('reportFilters.viewSavedReports module', function() {

    beforeEach(module('reportFilters.viewSavedReports'));

    describe('viewSavedReports controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var viewSavedReportsCtrl = $controller('ViewSavedReportsCtrl');
            expect(viewSavedReportsCtrl).toBeDefined();
        }));

    });
});